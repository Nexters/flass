import React, {Component, PropTypes} from 'react';

const propTypes = {};

const defaultProps = {};

class FlassContainer extends Component {
  constructor(props) {
    super(props);
  }

  componentDidMount() {
  }

  render() {
    return (
      <div>

      </div>
    );
  }
}

FlassContainer.propTypes = propTypes;
FlassContainer.defaultProps = defaultProps;

export default FlassContainer;
